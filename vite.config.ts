import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  // Use a root-relative base so assets work on custom domains and GitHub Pages
  base: "/"
})